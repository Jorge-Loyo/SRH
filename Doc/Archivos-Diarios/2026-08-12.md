# Plan de Sesión — 2026-08-12

**Tipo de sesión:** Documentación técnica  
**Rama:** `Desarrollo_Jorge`  
**Responsable:** Jorge

---

## Contexto

Sesión dedicada exclusivamente a documentación. No se realizaron cambios funcionales en el sistema.  
El estado del proyecto al inicio de la sesión es el resultado de la sesión `2026-09` (última sesión de desarrollo), con los siguientes ítems completados:

- M11 — Migración de `antiguedad` a `cargo_dotacion` ✅
- F10 — Panel KPIs en `DotacionTotalPage` ✅
- B11 — Endpoint `POST /api/dotacion/cargos/sincronizar` ✅
- M15 — Tablas `personas_dotacion` y `cargo_dotacion` ✅
- Fixes: ListaCargosPage, createAlta duplicate entry, Herramientas sidebar ✅
- Deploy DevOps: `Dockerfile` Python + servicio en `docker-compose.yml` ✅

**Pendiente de desarrollo:** F11 — Integración paso 6 en DotaneitorPage

---

## Objetivos de la sesión

Crear documentación técnica completa del proyecto en tres documentos nuevos dentro de `Doc/`.

---

## Tareas realizadas

### DOC-1 — Arquitectura y Onboarding ✅

**Archivo:** `Doc/ARQUITECTURA_ONBOARDING.md`

Documentación orientada a un desarrollador nuevo que se incorpora al proyecto.

| Sección | Contenido |
|---|---|
| Stack tecnológico | Node 18, Express, TypeORM, MySQL 8, React 18, Vite, Tailwind, FastAPI, JWT, Zod, Winston |
| Estructura de carpetas | Árbol completo `app/`, `frontend/`, `Doc/`, archivos raíz |
| Flujo de arranque | `server.js` → `bootstrap()` → DB → Express → schedulers |
| Módulos del backend | 10 módulos en `src/modules/` + 17 rutas legacy en `src/routes/` |
| Módulos del frontend | 10 grupos de páginas con rutas y roles por sección |
| Microservicio Python | Flujo de 10 pasos del Dotaneitor, proxy desde Node |
| Autenticación | JWT + refresh token rotativo, middlewares de seguridad |
| Flujo de datos | Alta de cargo y sincronización de dotación paso a paso |
| Variables de entorno | Todas las variables requeridas documentadas |
| Scripts disponibles | Backend, frontend y scripts de migración |
| CI/CD y Deploy | Pipeline GitLab, servicios Docker en producción |
| Convenciones de código | Módulos, validación, queries, errores, logs, commits |

---

### DOC-2 — Referencia de API REST ✅

**Archivo:** `Doc/API_REFERENCIA.md`

Documentación completa de todos los endpoints del backend Node y del microservicio Python.

| Grupo | Endpoints documentados |
|---|---|
| Auth | `POST /login`, `GET /me`, `POST /refresh`, `POST /logout` |
| Alta de Cargo | 15 endpoints: catálogos, CRUD altas, lista/export `new_cargo`, KPIs |
| Dotación | `GET /kpis`, `GET /estado`, `POST /sincronizar`, `GET /cargos/estado`, `POST /cargos/sincronizar` |
| Organigrama | `GET /organigrama` con árbol jerárquico por sigla o sección |
| Bajas Consolidadas | CRUD completo con auditoría |
| Seguimiento CPH | CRUD + estados únicos + stats por efector + by-baja |
| Seguimiento CEETPS | CRUD completo |
| Dotación Total | `GET /dotacion-total` con filtros y export Excel |
| POU | 7 endpoints: lista, períodos, hospitales, comparativa, export |
| Recorridas | CRUD completo |
| Usuarios | CRUD con permiso `can_manage_users` |
| Auditoría | Lista + purge |
| Herramientas | ERD, tablas, admin CRUD, proxy Dotaneitor |
| Microservicio Python | 11 endpoints: session, upload, normalizar, procesar, cruzar, preview, guardar-bd, descargar, reporte-calidad, historial, ultima-actualizacion |

Para cada endpoint se documentó: método HTTP, URL, roles requeridos, query params, body, respuesta exitosa, errores posibles.

---

### DOC-3 — Esquema de Base de Datos ✅

**Archivo:** `Doc/ESQUEMA_BASE_DE_DATOS.md`

Documentación completa del esquema de la base de datos `dotacion_db`.

| Sección | Contenido |
|---|---|
| Mapa general | 13 grupos de tablas con todas las tablas del sistema |
| Núcleo de cargos | `new_cargo`, `cargos_alta`, `registro_cph`, `registro_enf`, `registro_tec_pou`, `registro_tec_pof` |
| Catálogos | `carreras`, `modalidades`, `puestos_cargo`, `especialidades`, `jornadas`, `tipos_cargo`, `cargo_etiquetas` |
| Dotación normalizada | `personas_dotacion`, `cargo_dotacion` con invariante crítica documentada |
| Padrón procesado | `dot_resultado`, `dot_resultado_historial` |
| Tablas Dotaneitor | `dot_agrupador`, `dot_unificador_puestos`, `dot_especialidades` |
| Padrón legacy | `personas`, `cargos`, `roles` (PK compuesta por período) |
| Efectores | `siglas`, `organigramas`, `pou` |
| Módulo concursal | `bajas_consolidadas`, `seguimiento_cph`, `seguimiento_ceetps` |
| Seguridad | `users`, `permissions`, `module_permissions`, `custom_roles`, `refresh_tokens` |
| Auditoría | `audit_logs` |
| Contenido | `recorridas`, `minutas`, `conjuntos_config` |
| Diagrama de relaciones | Relaciones principales entre todas las tablas |
| Decisiones de diseño | 12 decisiones documentadas con justificación |
| Scripts de migración | Estado de M1 a M16 |

---

## Commit realizado

```
cbd3c95  docs: ARQUITECTURA_ONBOARDING, API_REFERENCIA, ESQUEMA_BASE_DE_DATOS
```

3 archivos nuevos — 2.455 líneas de documentación.

---

## Estado del proyecto al cierre de sesión

### Pendientes de desarrollo (sin cambios respecto al inicio)

| ID | Tarea | Prioridad |
|---|---|---|
| F11 | Integración paso 6 en DotaneitorPage (sincronizar dotación post-guardado) | Media |
| M10 fase 2 | Eliminar campos texto redundantes en `new_cargo` (`carrera`, `modalidad`, `puesto`, `especialidad`) | Baja — bloqueado por B8/B9/B10 |
| B8 | Reescribir filtros WHERE en `carrerasController.js` para usar FKs | Baja |
| B9 | Reescribir búsqueda full-text con FKs | Baja |
| B10 | Reescribir `uploadController.js` sin campos texto | Baja |
| F7 | Selector tipo TEC desde BD (`modalidad_tec`) | Baja |
| F9 | Export Excel en `ListaCargosPage` con FKs | Baja |

### Deuda técnica conocida

| Item | Descripción |
|---|---|
| 46.889 cargos sin `id_alta` | Cargos históricos sin evento de alta registrado |
| Campos texto en `new_cargo` | `carrera`, `modalidad`, `puesto`, `especialidad` duplican FKs |
| `situacion_revista` en `new_cargo` | Fuente de verdad migrada a `cargo_dotacion`, campo legacy mantenido |
| Usuarios/siglas hardcodeados | `concursalesHelpers.js` tiene asignaciones hardcodeadas |

---

## Próxima sesión sugerida

Retomar desarrollo funcional. Candidatos:

1. **F11** — Integración Dotaneitor paso 6 (sincronizar dotación): es el único pendiente del plan 2026-09, bajo riesgo, impacto directo en el flujo del usuario.
2. **Nuevo feature** — Definir con el equipo qué módulo o mejora tiene mayor prioridad de negocio.
